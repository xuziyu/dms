/*
package com.caili.mapper;

import com.caili.pojo.DmsMemberMaster;
import org.apache.ibatis.annotations.Insert;
import org.springframework.stereotype.Repository;

*/
/**
 * Create By Simple4H
 * Date: 2019-07-27 22:20
 *//*

@Repository
public interface DdmsMemberExtMasterMapper extends DdmsMemberMasterMapper{

    @Insert("insert into bns.ddms_member_master " +
            "values (#{id},#{distNo},#{distName},#{sponsorNo},#{sponsorName},#{levelNo},now(),#{pin},#{telephone},#{distStatus},now(),#{vipTime},#{svipTime})")
    Integer insertMember2Bns(DmsMemberMaster dmsMemberMaster);
}
*/
